services:
  producer:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: gtfsrt-tcar
    environment:
      - TZ=Europe/Paris
    ports:
      - "127.0.0.1:50001:3000"
    restart: unless-stopped
